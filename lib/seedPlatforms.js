/**
 * Seed default platforms (stubbed).
 *
 * In the full app this populates `platforms` / backfills related collections.
 * For local boot + Mongo connectivity debugging, we keep it idempotent
 * and safe as a no-op.
 */

async function seedPlatforms() {
  return;
}

module.exports = { seedPlatforms };

